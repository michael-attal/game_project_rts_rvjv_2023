using UnityEngine;

// REMARK: If there is a significant difference in the basic unit manager between Slime and Meca, we need to create two separate scripts, such as BasicSlimeUnitManagerWithoutECS and BasicMecaUnitManagerWithoutECS
public class BasicUnitManagerWithoutECS : MonoBehaviour
{
    public float speed;

    private void Start()
    {
    }


    private void Update()
    {
    }

    // TODO: If we decide to proceed with the prototype without ECS, we need to create the following functions: attack, damage, selectable, and movement here.
}